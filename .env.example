# Copy to .env and fill in. .env is gitignored -- never commit real keys.

# Answer generation (generator.py). Retrieval works without it; generation does not.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. LlamaCloud/LlamaParse -- document parsing only, unused by default.
# LLAMA_CLOUD_API_KEY=llx-...
