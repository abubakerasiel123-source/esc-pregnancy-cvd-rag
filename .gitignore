# Credentials -- never commit.
.env
.streamlit/secrets.toml

# Source document: the 2025 ESC guideline is copyrighted by the European
# Society of Cardiology, so it is not redistributed here. See the README for
# where to obtain it.
pregnancy_cvd_2025.pdf

# The prebuilt Chroma index IS committed: it is what lets the deployed app
# work without the PDF, which is not redistributed here. Regenerate it with
# `python biko.py --build`.
__pycache__/
*.pyc

# Editor / backup noise.
*.bak
.DS_Store
.vscode/
